/**
 * Enum representing the type of transaction in the bank account.
 * It can be either DEPOSIT (for adding money) or WITHDRAWAL (for taking money out).
 */

public enum TransactionType {
    DEPOSIT, WITHDRAWAL
}
